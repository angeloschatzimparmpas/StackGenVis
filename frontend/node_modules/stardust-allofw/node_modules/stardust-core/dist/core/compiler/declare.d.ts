import { Specification } from "../spec/spec";
export declare class CustomMarkItem {
    private _name;
    private _attrs;
    constructor(name: string);
    attr(name: string, expression: string): CustomMarkItem;
    generateCode(): string;
}
export declare class CustomMark {
    private _imports;
    private _inputs;
    private _variables;
    private _items;
    constructor();
    input(name: string, type: string, initial?: string): CustomMark;
    variable(name: string, expression: string): CustomMark;
    add(name: string): CustomMarkItem;
    generateCode(markName: string): string;
    compile(): Specification.Mark;
}
