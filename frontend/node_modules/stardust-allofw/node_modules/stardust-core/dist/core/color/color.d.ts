import { MathType } from "../math/math";
export declare class Color extends MathType {
    r: number;
    g: number;
    b: number;
    a: number;
    constructor(r?: number, g?: number, b?: number, a?: number);
    toArray(): number[];
    clone(): Color;
    static FromArray([r, g, b, a]: number[]): Color;
    static FromHTML(html: string): Color;
}
export declare module color {
    function fromHTML(html?: string, alpha?: number): number[];
}
