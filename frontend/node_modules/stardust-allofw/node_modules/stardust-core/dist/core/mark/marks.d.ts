import { CustomMark } from "../compiler/declare";
import { Mark } from "./mark";
import { Specification } from "../spec/spec";
import { Platform } from "../platform/platform";
export declare module mark {
    function create(spec: Specification.Mark, shader: Specification.Shader, platform: Platform): Mark;
    function create(spec: Specification.Mark, platform: Platform): Mark;
    function create(spec: CustomMark, platform: Platform): Mark;
    function custom(): CustomMark;
    function compile(code: string): Specification.Marks;
    function circle(sides?: number): Specification.Mark;
    function rect(): Specification.Mark;
    function line(): Specification.Mark;
    function wedge(sides?: number): Specification.Mark;
    function polyline(): Specification.Mark;
}
export declare module shader {
    function compile(code: string): Specification.Marks;
    function basic(): Specification.Shader;
    function lighting(): Specification.Shader;
}
